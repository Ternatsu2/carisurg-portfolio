# Generated artefacts

`python scripts/train.py --config config.yaml` writes the fitted model and its
reproduction metrics here. Those generated files stay local and are excluded
from Git because the repository records the code, configuration, and reviewed
benchmark instead.
